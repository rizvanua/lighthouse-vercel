{
    "version": 2,
    "buildCommand": "cd api && npm install && npm run build",
    "outputDirectory": "api/dist",
    "functions": {
      "api/**/*.ts": {
        "memory": 3009,
        "maxDuration": 180
      }
    },
    "routes": [      
      {
        "src": "/api/analyze",
        "dest": "/api/analyze.ts"
      }
    ]
  }
